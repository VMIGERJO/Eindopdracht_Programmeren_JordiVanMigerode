<Window x:Class="EindOpdrachtGentseFeesten.PresentationWPF.EvenementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EindOpdrachtGentseFeesten.PresentationWPF"
        mc:Ignorable="d"
        Title="EvenementWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="ErrorTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Width" Value="300" />
            <Setter Property="Height" Value="50" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width ="*"/>
            <ColumnDefinition Width ="*"/>
        </Grid.ColumnDefinitions>
        <TreeView Name="EvenementenTrv" TreeView.SelectedItemChanged="EvenementenTrv_SelectedItemChanged" TreeViewItem.Expanded="EvenementTrvItem_Expanded">
        </TreeView>
        
    </Grid>
</Window>
